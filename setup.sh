#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

printf '%s\n' "openclaw-bridge setup is not implemented yet."
printf '%s\n' "Repository root: ${script_dir}"
printf '%s\n' "Next step: scaffold the SillyTavern plugin, bridge, and launcher scripts."
